import styled from 'styled-components';
// import { fontSize } from '../../global/fonts/GlobalFonts';

const S = {
    Wrapper: styled.div`
        display: flex;
        flex-wrap: wrap;
    `,
};

export default S;