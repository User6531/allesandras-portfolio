import styled from 'styled-components';
// import { fontSize } from '../../global/fonts/GlobalFonts';

const S = {
    Wrapper: styled.div`
        display: flex;
        width: 100%;
        height: 100%;
    `,
};

export default S;